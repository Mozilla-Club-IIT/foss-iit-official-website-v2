export interface BlogFrontMatter {
    title: string;
    date: string;
    description: string;
}

export interface Blog {
    title: string;
    date: string;
    description: string;
    blogId: string;
}
